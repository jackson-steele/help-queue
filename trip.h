#pragma once

#include <iostream>
#include <string>

using namespace std;

class trip
{
public:
  trip();
  ~trip();
  PlanTrip(string origin, string destination, string startDate, string endDate, double proposedBudget, int numPeople);
private:

};
